# yaml-language-server: $schema=../schema/config.schema.yaml
name: madara

accept:
  selectors:
    - "[href*=madara]"
    - "[content*=madara]"
    - "script#madara-core-js"
    - "[src*=madara]"
    - body.wp-manga-template-default
  hostnames:
    - isekaiscan.top

manga:
  title:
    - selector: div.post-title h1
      options:
        fix_capitalization: title
    - selector: div#manga-title h1
      options:
        text_selection:
          type: own-text
        fix_capitalization: title
  description:
    - selector: div.summary__content > p
      options:
        text_selection:
          type: own-text
    - selector: div.summary__content
      options:
        text_selection:
          type: all-text
          join_with: " "
    - selector: h5:contains(Summary) + div p
      options:
        text_selection:
          type: all-text
          join_with: " "
  cover_url:
    selector: div.summary_image img
    options:
      text_selection:
        type: attributes
        attributes:
          - data-src
          - src

  status:
    - div.post-status div + div div.summary-content
    - div.post-status div.summary-content
  authors: div.author-content a
  genres:
    selector: div.genres-content a
    options:
      fix_capitalization: title
      cleanup:
        - replace_regex: "Scifi"
          replace_with: "Sci-Fi"
  alt_titles: div.summary-heading:has('h5:icontains("alternative")') + div

  chapter:
    base:
      - li.wp-manga-chapter
      - div.json-item
    title:
      - a
      - selector: div.json-item
        options:
          text_selection:
            type: attributes
            attributes:
              - data-chapter_name
    url:
      - selector: a
        options:
          text_selection:
            type: attributes
            attributes:
              - href
              - src
      - selector: div.json-item
        options:
          text_selection:
            type: attributes
            attributes:
              - data-__url
    number:
      - span.chapter-number
      - selector: div.json-item
        options:
          text_selection:
            type: attributes
            attributes:
              - data-chapter_num
    date:
      - span.chapter-release-date i
      - selector: span.chapter-release-date a
        options:
          text_selection:
            type: attributes
            attributes:
              - title
      - selector: div.json-item
        options:
          text_selection:
            type: attributes
            attributes:
              - data-updated_at

    fetch_external:
      - id: script:icontains('mangaid')
        regex: var mangaID = '(?<id>\d+)';
        url: /ajax-list-chapter?mangaID={id}

      - id:
          selector: "link[rel=canonical][href*=mangasushi], link[rel=canonical][href*=manga68]"
          options:
            text_selection:
              type: attributes
              attributes:
                - href
        regex: /manga/(?<id>[^/]+)/
        url: https://{host}/manga/{id}/ajax/chapters/
        method: post

      - id:
          selector: "#manga-chapters-holder"
          options:
            text_selection:
              type: attributes
              attributes:
                - data-id
        regex: (?<id>.*)
        url: "{url}ajax/chapters/"
        method: post

      - id: script:icontains('comicSlug')
        regex: comicSlug = '(?<id>[^']+)';
        url: "https://{host}/api/comics/{id}/chapters"
        json_array: /data/chapters
        json_url_template: "{url}/{chapter_slug}"

images:
  image_selector:
    - selector: img.wp-manga-chapter-img, li.blocks-gallery-item img, div.reading-content div.text-left img
      options:
        text_selection:
          type: attributes
          attributes:
            - data-src
            - src
        text_split_regex: null
    # manhuaplus.com's "manhua"/video-type chapter template (e.g. martial-peak) renders
    # plain <img src> tags in this wrapper instead of the usual .wp-manga-chapter-img -
    # the manga's other/older chapters still use the selector above untouched.
    - selector: div.chapter-video-frame img
      options:
        text_selection:
          type: attributes
          attributes:
            - data-src
            - src
        text_split_regex: null
    - selector: p#arraydata
      options:
        text_split_regex: ","

search:
  - hostnames:
      - lhtranslation.net
      - manga68.com
      - manhuaplus.com
      - manhwatop.com
      - manhuafast.com
    search_url: "{hostname}/?s={query}&post_type=wp-manga"
    query_format:
      - replace_regex: '\+'
        replace_with: "%2B"
      - replace_regex: " "
        replace_with: "+"
    selectors:
      base: ".c-tabs-item__content"
      url:
        selector: a
        options:
          text_selection:
            type: attributes
            attributes:
              - href
              - src
      title:
        selector: h3 a
        options:
          fix_capitalization: title
      cover_url:
        selector: img
        options:
          text_selection:
            type: attributes
            attributes:
              - data-src
              - src
      posted: div.post-on span

date_formats:
  - "%b %e, %R"
  - "%e %B، %Y"
  - "%b-%d-%H:%M"
